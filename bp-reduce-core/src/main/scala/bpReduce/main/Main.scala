package bpReduce
package main

object Main {

  // main loop:
  // transform program, simplify expressions and simplify program
  // => adding a cache for negative results (positive makes no sense since the program has already been accepted)
  // might increase speed but then skips etc should be removed
}
